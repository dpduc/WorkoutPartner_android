package com.workoutpartner.core.repcounting

/**
 * The five movement types the pose-tracking engine recognizes and can count
 * Reps for (per CONTEXT.md's Exercise definition).
 */
enum class Exercise {
    SQUAT,
    PUSH_UP,
    SIT_UP,
    LUNGE,
    JUMPING_JACK,
}
